import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starterone',
  appName: 'website',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
